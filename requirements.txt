# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy>=2.0.36
aiosqlite>=0.20.0
asyncpg>=0.30.0
alembic>=1.14.0
psycopg2-binary>=2.9.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.18

# Broker Integrations
robin-stocks>=3.0.0
plaid-python>=26.0.0

# Encryption
cryptography>=42.0.0

# Caching
redis>=3.5.0

# HTTP Client
httpx>=0.28.0

# Rate Limiting
slowapi>=0.1.9

# Data Processing
pandas>=1.5.0
numpy>=1.24.0

# Market data (quotes, candles, company info). Unofficial Yahoo wrapper  - 
# used by app/stocks/service.py.
yfinance>=0.2.50

# Logging
python-json-logger>=3.0.0

# Email
aiosmtplib>=2.0.0
email-validator>=2.0.0

# AI Assistant
# pydantic-ai-slim carries the google (Gemini), mcp, and duckduckgo optional extras.
# ``openrouter`` is kept for backwards-compat; drop if you don't need it.
pydantic-ai>=1.0.0,<2
pydantic-ai-slim[google,openrouter,mcp,duckduckgo]>=1.0.0,<2
fastmcp>=3.0.0

# Agent evaluation harness (evals/) - same team as pydantic-ai, native Dataset/Case/Evaluator support.
pydantic-evals>=1.0.0

# Observability (Pydantic Logfire)
# Instruments FastAPI, SQLAlchemy (via asyncpg), HTTPX, PydanticAI, and system metrics.
logfire[fastapi,sqlalchemy,httpx,asyncpg,system-metrics]>=4.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0

# Dev Tools
black>=24.10.0
ruff>=0.8.0
mypy>=1.13.0
